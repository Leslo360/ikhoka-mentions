{
  "devDependencies": {
    "@types/node": "^14.11.2",
    "gts": "^3.1.0",
    "nodemon": "^2.0.16",
    "typescript": "^4.0.3"
  },
  "scripts": {
    "start": "nodemon build/src/index.js",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc --watch",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  }
}
